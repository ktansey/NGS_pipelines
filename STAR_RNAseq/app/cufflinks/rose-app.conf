[command]
cufflinksFPKM=cufflinks --library-type fr-firststrand --GTF ${WORKING_DIR}/${PROJECT_NAME}/${RESOURCES_DIR}/${GTF_NAME} -o ${WORKING_DIR}/${PROJECT_NAME}/${OUTPUT_DIR}/${SAMPLE_ID}_accepted_hits.bam 
cuffDiff=cuffdiff -o ${WORKING_DIR}/${PROJECT_NAME}/${OUTPUT_DIR}/  -L \${TEST}\,\${CONTROL} ${WORKING_DIR}/${PROJECT_NAME}/${RESOURCES_DIR}/${GTF_NAME} ${WORKING_DIR}/${PROJECT_NAME}/${OUTPUT_DIR}/${TEST}_accepted_hits.bam  ${WORKING_DIR}/${PROJECT_NAME}/${OUTPUT_DIR}/${CONTROL}_accepted_hits.bam 