[command]
remove1="""rm ${WORKING_DIR}/${PROJECT_NAME}/${ALIGN_DIR}/${FIRST}.sam; rm ${WORKING_DIR}/${PROJECT_NAME}/${ALIGN_DIR}/${FIRST}.bam; rm ${WORKING_DIR}/${PROJECT_NAME}/${ALIGN_DIR}/${FIRST}.s.bam; rm ${WORKING_DIR}/${PROJECT_NAME}/${ALIGN_DIR}/${FIRST}.s.bam.bai; rm ${WORKING_DIR}/${PROJECT_NAME}/${ALIGN_DIR}/${SECOND}.sam; rm ${WORKING_DIR}/${PROJECT_NAME}/${ALIGN_DIR}/${SECOND}.bam; rm ${WORKING_DIR}/${PROJECT_NAME}/${ALIGN_DIR}/${SECOND}.s.bam; rm ${WORKING_DIR}/${PROJECT_NAME}/${ALIGN_DIR}/${SECOND}.s.bam.bai"""