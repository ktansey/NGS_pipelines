[command]
rm_blacklist=bedtools subtract -a ${WORKING_DIR}/${PROJECT_NAME}/${OUTPUT_DIR}/${TEST}_${CONTROL}_narrow_summits.bed -b /scratch/mpnkt/reference_data/annotation/wgEncodeDacMapabilityConsensusExcludable.bed > ${WORKING_DIR}/${PROJECT_NAME}/${OUTPUT_DIR}/${TEST}_${CONTROL}_narrow_summits_filtered.bed